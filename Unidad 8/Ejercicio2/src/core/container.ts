import 'reflect-metadata';
import { Container } from 'inversify';
import TYPES from './types';

const container = new Container();

// ⏳ Iremos añadiendo bindings conforme creemos las clases

export default container;